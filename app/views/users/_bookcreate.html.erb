<div class="book_create_bar">
  <h2>New book</h2>
  <%= form_for(book, url: books_path) do |f| %>

    <div class="field row">
      <%= f.label :title %>
      <%= f.text_field :title, :class => 'col-xs-12' %>
    </div>
    <div class="field row">
      <%= f.label :opinion %>
      <%= f.text_area :body, :class => 'col-xs-12'%>
    </div>
    <div class="actions row">
      <%= f.submit 'Create Book', :class => 'btn btn-primary col-xs-12' %>
    </div>
  <% end %>
</div>